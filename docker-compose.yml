services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: app.netbird.cloud:8081/customer/stable:1.0.0
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: always